#!/bin/sh

# script/server: Start application for web environment

set -e

cd "$(dirname "$0")/.."

echo "==> Compiling client code"
rm -rf dist/
npm run build

echo "==> Starting server"
node ./../server
